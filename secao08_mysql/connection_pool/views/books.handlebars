<h1>Livros disponíveis</h1>

<div class="books-container">
    <a href="/">Adicionar novo livro</a>

    {{#each books}}
    <div>
        #{{ this.id }} - <span class="bold"><a href="/books/{{ this.id }}">{{ this.title }}</a></span> - {{ this.pageqtd }} páginas
    </div>
    {{/each}}
</div>